# Set this in Vercel: Project Settings → Environment Variables
# Name: VITE_API_URL
# Value: the Railway DeliveryApp public URL (no trailing slash)
# Apply to: Production, Preview, and Development
# Then Redeploy the frontend. Vite reads this only at build time.
VITE_API_URL=
